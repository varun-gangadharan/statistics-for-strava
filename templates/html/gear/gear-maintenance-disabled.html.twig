{% extends "html/gear/gear-maintenance.html.twig" %}

{% block content %}
    {% autoescape false %}
        <div class="grid grid-cols-1 gap-4">
            <div class="mb-4 p-4 bg-white border border-gray-200 rounded-lg shadow-xs sm:p-6">
                <h3 class="mb-4 text-lg font-semibold">
                    {{ "Gear maintenance"|trans }}
                </h3>
                <div class="flex items-center p-4 mb-4 text-sm text-blue-800 border border-blue-300 rounded-lg bg-blue-50" role="alert">
                    <svg class="shrink-0 inline w-4 h-4 me-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5ZM9.5 4a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3ZM12 15H8a1 1 0 0 1 0-2h1v-3H8a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1v4h1a1 1 0 0 1 0 2Z"/>
                    </svg>
                    <span class="sr-only">Info</span>
                    <div>
                        {% set configFile = '<span class="italic">storage/gear-maintenance/config.yml</span>' %}
                        <span class="font-medium">{{ "This feature is currently disabled"|trans }}.</span>
                        {{ "To enable it, configure {configFile} and rebuild the files for the changes to take effect"|trans({'{configFile}': configFile}) }}.
                        {{ "Detailed instructions are available in the documentation on"|trans }} <a href="https://github.com/robiningelbrecht/statistics-for-strava/wiki/Gear-maintenance" class="font-semibold underline hover:no-underline">the wiki</a>.
                    </div>
                </div>
            </div>
        </div>
    {% endautoescape %}
{% endblock %}