flysystem:
    storages:
        public.storage:
            adapter: 'memory'

        file.storage:
            adapter: 'memory'

        database.storage:
            adapter: 'memory'

        build.storage:
            adapter: 'memory'