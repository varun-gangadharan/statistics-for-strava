flysystem:
    storages:
        default.storage:
            adapter: 'memory'

        public.storage:
            adapter: 'memory'

        gear-maintenance.storage:
            adapter: 'memory'

        file.storage:
            adapter: 'memory'

        database.storage:
            adapter: 'memory'

        build.storage:
            adapter: 'memory'